/*CONSOLA JAVASRIPT*/

/*
assert() -> La funcionalida de esta es mostrar un error por consola
si la afirmacion es falsa. Si la afirmacion es verdadera no aparecera nada

clear() -> La funcionalida de esta es limpiar la consola.

error() -> Este tiene como fin mostrar error en la consola podemos
ejecutar cualquier error en siertos casos y mostrarlo por la consola.

info() -> Esto es para mostrar por consola un mensaje dependiendo del caso.

log() -> Este tiene la misma funcionalidad practicamente que info ya que muestran
por consola un mensaje.

table() -> La funcionalidad es mostrar por pantalla una tabla y dentro de 
esta tabla se mostrara un array que nosotros le indiquemos mostrara el indice
y el contenido que tiene por consola de una manera mucho mas grafica.

warn() -> Su funcionalidad es mostrar en mensaje en amarillo que indica alerta 
y este se muestra a traves de la consola.

dir() -> Su funcionaliad es mostrar por consola variables o lo que sea pero de una manera
mas descriptiva diciendo el tipo y muchas mas funcionalidades extra.

FUNCIONES DE CONTEO

count() -> La funcionalidad es decir cuantas veces ha sido utilizada entre mas escribamos count
mas se mostrara cada vez que escribamos console.count se hira sumando y mostrando la suma

countReset() -> Este reinicia el ciclo de count.

FUNCIONES DE AGRUPACION

group() -> console.group permite crear un grupo en consola que podemos abrir o cerrar
todo lo que escribamos por consola estara dentro de este grupo, este va a estar directamente
abierto automaticamente y todo lo que escribamos esatara dentro.

groupEnd() -> este nos permite tanto entrar o salir de el ultimo grupo creado.

groupCollapsed() -> ha diferencia de console.group este aunque tambien crea un grupo su funcionalidad
es crear un grupo pero automaticamente estara este cerrado a diferencia de group que automaticamente todo
el grupo esta abierto y todo lo que copiemos estara dentro de este grupo.  

FUNCIONES DE TIEMPO

time() -> Inizia Un temporizador automaticamente en la consola.

timeLog() -> Muestra el tiempo que ha pasado desde que se inizio el temporizador.

timeEnd() -> Este finaliza el temporizador y mustra en cuanto tiempo finalizo.

si queremos utilizar codigo css en consola debemos hacer lo siguiente utilizar (%c+Texto)

console.log("%cESTO ES TEXTO","codigo css que pondremos")

hay que tener en cuenta que este tiene ciertas limitaciones como que no se puede cambiar el tamaño.

*/